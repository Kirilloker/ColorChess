version: '3.8'

services:
  gameserver:
    build:
      context: .
      dockerfile: Dockerfile.gameserver
    ports:
      - "11000:11000"
    environment:
      IPDB: db
      USERDB: kirillok
      PASSWORDDB: loki5566
      NAMEDB: colorchessdb

  site:
    build:
      context: .
      dockerfile: Dockerfile.site
    ports:
      - "3000:3000"
      - "80:80"
    environment:
      URL_API: "gameserver:11000"

  reportbottelegram:
    build:
      context: .
      dockerfile: Dockerfile.reportbottelegram
    environment:
      SERVER_URL: "http://gameserver:11000"

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: "loki5566"
      MYSQL_DATABASE: "colorchessdb"
      MYSQL_USER: "kirillok"
      MYSQL_PASSWORD: "loki5566"
    ports:
      - "3307:3306"
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:
