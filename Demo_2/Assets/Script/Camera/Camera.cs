using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Camera : MonoBehaviour
{
    /*
    private DeviceOrientation _deviceOrientation = DeviceOrientation.Unknown;

    void Update()
    {
        if (_deviceOrientation == Input.deviceOrientation) return;

        _deviceOrientation = Input.deviceOrientation;

        // Смещение камеры в зависимости от наклона телефона 

        if (_deviceOrientation == DeviceOrientation.Portrait ||
            _deviceOrientation == DeviceOrientation.PortraitUpsideDown)
        {
            // Камера перевернулась на Вертикальный вид
        }
        else if (_deviceOrientation == DeviceOrientation.LandscapeLeft ||
                 _deviceOrientation == DeviceOrientation.LandscapeRight) 
        {
            // Камера перевернулась на горизонтальный вид
        }
    }
    */
}
