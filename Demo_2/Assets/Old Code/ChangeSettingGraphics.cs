using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ChangeSettingGraphics : MonoBehaviour
{
    /*
    public Toggle swith;

    public static bool LowGraphics;

    public Light GameDesk;

    public Material BoarMaterial;

    public CameraManager cameraManager;

    private void Awake()
    {
        ChangeGraphics();
    }

    public void ChangeGraphics() 
    {
        LowGraphics = swith.isOn;

        SwitchShadow();
        SwitchSmoothness();
        SwitchCameraSpeed();

        cameraManager.Set_Camera_Change_Speed(0f);
    }

    public void SwitchShadow() 
    {
        if (LowGraphics == true) 
            GameDesk.shadows = LightShadows.None;
        else 
            GameDesk.shadows = LightShadows.Soft;
        
    }

    public void SwitchSmoothness() 
    {
        if (LowGraphics == true)
            BoarMaterial.SetFloat("_Glossiness", 0f);
        else
            BoarMaterial.SetFloat("_Glossiness", 0.5f);
    }

    public void SwitchCameraSpeed() 
    {
        if (LowGraphics == true)
            cameraManager.Set_Camera_Change_Speed(0f);
        else
            cameraManager.Set_Camera_Change_Speed(2f);
    }
    */
}
